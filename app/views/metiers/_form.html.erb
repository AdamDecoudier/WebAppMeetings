<%= form_with(model: metier, local: true) do |form| %>
  <% if metier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(metier.errors.count, "error") %> prohibited this metier from being saved:</h2>

      <ul>
      <% metier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_field :label, id: :metier_label %>
  </div>

  <div class="field">
    <%= form.label :specialite %>
    <%= form.text_field :specialite, id: :metier_specialite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
